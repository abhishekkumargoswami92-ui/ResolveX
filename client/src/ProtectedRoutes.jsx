// MOCKED Protected Routes — SAFE, NO AUTH, NO CONTEXT

export const ProtectedRoute = ({ children }) => {
  return children;
};

export const StudentRoute = ({ children }) => {
  return children;
};

export const AdminRoute = ({ children }) => {
  return children;
};
